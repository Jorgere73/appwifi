#include "selectorbestworst.h"
#include <stdio.h>
#include <stdlib.h>

void wificollector_select_best(int** source)
{

}

int searchinstring(int* str, int size)
{
    char* string = calloc(size, sizeof(char));
    for(int i = 0; i < size; i++)
    {
        string[i] = str[i] + '0';
    }
    
    return 0;
}